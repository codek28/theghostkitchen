// can update delivered on online orders (online-orders are completed on delivery)
// can update delivered on cod order/on-call order that will push notif to staff for entry (cod and on-call orders are completed on approval)

export const Deliveryman = () => {

    return (
      <div>
          Use Carousel
      </div>
    );
  };